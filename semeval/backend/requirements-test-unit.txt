# Minimal dependencies for running unit tests only.
# Does NOT include audio, ASR, or LLM dependencies.
# Use pyproject.toml [dev] for the full install.

fastapi>=0.111.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
sqlalchemy[asyncio]>=2.0.30
pytest>=8.2.0
pytest-asyncio>=0.23.7
structlog>=24.2.0
